# Django Environment Configuration
# 复制此文件为 .env 并根据环境配置相应的值

# =============================================================================
# 环境选择 (必需)
# =============================================================================
# 环境类型: development, production
DJANGO_ENVIRONMENT=development

# =============================================================================
# 生产环境必需配置 (仅在 DJANGO_ENVIRONMENT=production 时需要)
# =============================================================================

# Django 密钥 (生产环境必须设置，开发环境有默认值)
SECRET_KEY=your-secret-key-here

# 允许的主机 (生产环境必须设置，多个主机用逗号分隔)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# 数据库配置 (生产环境使用PostgreSQL)
DB_NAME=ops_job
DB_USER=ops_job_user
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# Redis 配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# CORS 配置 (生产环境，多个域名用逗号分隔)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# CSRF 信任的来源 (生产环境，多个域名用逗号分隔)
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# 静态文件和媒体文件路径 (可选，有默认值)
STATIC_ROOT=/var/www/ops_job/static
MEDIA_ROOT=/var/www/ops_job/media

# 邮件配置 (生产环境)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=your-email@gmail.com
SERVER_EMAIL=your-email@gmail.com
ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# 开发环境配置 (可选，有默认值)
# =============================================================================

# Redis 配置 (开发环境，如果不设置密码可以留空)
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=

# =============================================================================
# 使用说明
# =============================================================================

# 1. 开发环境:
#    - 复制此文件为 .env
#    - 设置 DJANGO_ENVIRONMENT=development
#    - 其他配置可以使用默认值或根据需要调整
#    - 开发环境使用SQLite数据库，无需配置PostgreSQL

# 2. 生产环境:
#    - 复制此文件为 .env
#    - 设置 DJANGO_ENVIRONMENT=production
#    - 必须设置所有标记为"生产环境必需"的配置项
#    - 生产环境使用PostgreSQL数据库

# 3. 环境变量加载:
#    - 可以使用 python-dotenv 包自动加载 .env 文件
#    - 或者直接在系统中设置环境变量

# 4. 设置模块更新:
#    - 现在使用 ops_job.settings 包而不是单个文件
#    - 根据 DJANGO_ENVIRONMENT 自动选择相应的配置
